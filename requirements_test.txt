# Test dependencies for the SmartHomeSec Home Assistant integration.
# Home Assistant itself (plus requests/certifi) is pulled in transitively by
# pytest-homeassistant-custom-component, which pins a matching HA version.
pytest-homeassistant-custom-component
pytest-cov

# Runtime dependency of the integration. Home Assistant does NOT ship this, so
# it is declared in manifest.json "requirements" -- keep this pin in sync with
# the manifest so CI tests the exact version HA will install.
websocket-client==1.9.0
