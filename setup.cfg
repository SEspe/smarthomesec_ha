[tool:pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Ensure the repo root is importable so `custom_components.smarthomesec` resolves
# the same way it does when Home Assistant loads the integration.
pythonpath = .
addopts = -q
